# Rust build artifacts
/target
**/*.rs.bk

# Cargo.lock is ignored (library crate; consumers resolve their own versions)
Cargo.lock

# Editor / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Local-only design / analysis docs (kept in the working tree, not published)
/docs/WAFFO_RS_DESIGN.md
/docs/WAFFO_GO_ANALYSIS.md

# Real credentials — never commit. Use .env.example as the template.
/.env
