# Rust build artifacts
/target
**/*.rs.bk

# Cargo.lock is ignored (library crate; consumers resolve their own versions)
Cargo.lock

# Editor / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Local-only docs / references (design notes, vendor test-case sheets, etc.)
/docs/

# Real credentials — never commit. Use .env.example as the template.
/.env
